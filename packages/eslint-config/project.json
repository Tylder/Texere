{
  "name": "eslint-config",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/eslint-config",
  "projectType": "library",
  "targets": {
    "check-types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"skip type checking for eslint-config package (config-only)\""
      },
      "cache": false
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"skip lint for eslint-config package (config-only)\""
      },
      "cache": false
    }
  }
}
