# Texere v1.1: Schema & Behavior Refinement

## TL;DR

> **Quick Summary**: Simplify Texere's type system (17→6 node types with facets, 14→12 edge types),
> enhance search (BM25 exposure, FTS5 power features, tag/type flexibility), add batch operations
> and workflow helpers (`replace_node`). Hard cutover, no backward compatibility.
>
> **Deliverables**:
>
> - Simplified type system with faceted node schema
> - Refined edge types validated against real documents
> - Enhanced search with explainability and full FTS5 power
> - Batch create support for nodes and edges
> - New `texere_replace_node` atomic helper tool
> - Updated SKILL.md for agent consumption
>
> **Estimated Effort**: Large **Parallel Execution**: YES - 5 waves **Critical Path**: Task 1 → Task
> 2 → Tasks 3/4/5 → Task 6 → Task 7

---

## Context

### Original Request

Texere v1.1 — a schema and behavior refinement, NOT a rewrite. Simplify the type system for better
agent UX, enhance search capabilities, and add missing workflow tools. Hard cutover, no backward
compatibility, no API versioning.

### Interview Summary

**12 decisions made across extended consultation:**

1. Node types: 17 flat types → 6 core types + required `role` facet with strict constraint matrix
2. Batch creates: Accept single object OR array, always atomic
3. Search explainability: Expose BM25 rank, match_quality, match_fields
4. Relationship summary: Edge count aggregation per search result
5. Minimal response: `minimal: true` returns only `{ id }`
6. Duplicate warning: Non-blocking fuzzy title check before insert
7. Edge types: 14 → 12 (validated against real documents by two independent Oracle agents)
8. SKILL.md: Mandatory search-before-create workflow (already implemented)
9. FTS5 sanitizer: Relax to allow phrases, booleans, grouping with fallback
10. Tag OR logic: `tag_mode: "all" | "any"` parameter
11. Multi-type search: `type` accepts string or array
12. `replace_node`: Atomic create + REPLACES edge + invalidate

**Key constraints (user's words):**

- "lets not call this v2, but instead v1.1, v2 comes later"
- "no need to version the api"
- "no backward compatibility needed"
- "atomic MUST always be true, all or nothing"
- "accept array, I didnt say it had to be an array"
- "edges is what make the Texere ok or great"

### Research Findings

- BM25 rank computed at `search.ts:67` but stripped at `search.ts:77` — easy fix
- FTS5 sanitizer (`sanitize.ts:8`) strips ALL boolean/phrase operators — overcorrecting
- Tag filtering AND-only (`search.ts:44-52`), type filtering single-type (`search.ts:20-22`)
- `createEdge` auto-invalidates on `DEPRECATED_BY` (`edges.ts:81`) — must port to `REPLACES`
- `storeNode` auto-creates `file_context` nodes for `anchor_to` (`nodes.ts:165-189`) — needs new
  type mapping
- Tag-only search (empty query + tags) returns empty results — bug (`search.ts:10-12`)
- Live data: 20 nodes across 9 types, 7 edges across 7 types (including 1× `RELATED_TO` and 1×
  `MOTIVATED_BY` — both being removed)
- FTS5 virtual table cannot be ALTERed — must DROP + CREATE + repopulate

### Metis Review

**Identified gaps (all addressed):**

- Type-role constraint matrix must be enforced at graph layer, not just Zod
- `anchor_to` auto-created nodes need new type mapping (→ `artifact`/`file_context`)
- `REPLACES` auto-invalidation must be ported from `DEPRECATED_BY` behavior
- FTS5 rebuild is mandatory (cannot ALTER virtual tables)
- New `SearchResult` return type needed (distinct from `Node`)
- Tag-only search bug (E5) must be fixed — currently returns empty
- Batch size capped at 50 (SQLite `SQLITE_MAX_VARIABLE_NUMBER` limit)
- Empty batch array → validation error
- `minimal: true` suppresses duplicate warnings (you asked for minimal, you get minimal)
- Migration needed for existing 20 nodes + 7 edges

---

## Work Objectives

### Core Objective

Refine Texere's schema and behavior to reduce agent decision burden, enhance search power, and add
missing workflow tools — all while preserving the core architectural strengths (immutability, typed
edges, JSON-first responses, `about` tool).

### Concrete Deliverables

- `packages/graph/src/types.ts` — new enums: 6 NodeTypes, 12 EdgeTypes, NodeRole, facet types
- `packages/graph/src/schema.ts` — new DDL with facet columns and rebuilt FTS5
- `packages/graph/src/nodes.ts` — facets, batch, minimal, duplicate warning
- `packages/graph/src/edges.ts` — new types, REPLACES behavior, batch, minimal
- `packages/graph/src/search.ts` — explainability, tag_mode, multi-type, tag-only fix
- `packages/graph/src/sanitize.ts` — relaxed with fallback
- `packages/graph/src/replace-node.ts` — new atomic helper
- `apps/mcp/src/tools/*.ts` — updated Zod schemas, new `replace-node.ts` tool
- `skills/texere/SKILL.md` — updated documentation for all changes
- Migration script for existing data

### Definition of Done

- [ ] `pnpm turbo test:unit` passes with zero failures
- [ ] All 6 node types create successfully with valid roles
- [ ] All 12 edge types create successfully
- [ ] Invalid type-role combinations rejected at graph layer
- [ ] Batch creates work atomically (all-or-nothing verified)
- [ ] Search returns rank, match_quality, match_fields, relationships
- [ ] FTS5 phrase and boolean queries work
- [ ] `replace_node` atomically replaces in one call
- [ ] SKILL.md documents all new types, edges, tools, and parameters

### Must Have

- 6 node types + role facet with strict constraint matrix
- 12 edge types (validated set)
- Batch create for both nodes and edges (array input, always atomic)
- Search explainability fields
- FTS5 sanitizer relaxation with graceful fallback
- `replace_node` atomic helper
- Migration of existing data

### Must NOT Have (Guardrails)

- **No facets beyond the 4 defined** (role, source, status, scope) — each facet means schema
  columns, FTS5 changes, trigger changes, Zod schemas, SKILL.md docs
- **No duplicate detection beyond FTS5 title search** — no Levenshtein, no trigrams, no embeddings
- **No `texere_about` changes** beyond what type/edge enum changes require
- **No general `update_node` tool** — `replace_node` is create+link+invalidate only
- **No stats tool expansion** — update type/edge enums only, no facet breakdowns
- **No SKILL.md pedagogical restructure** — update reference tables and examples only
- **No embedding changes** — `embedding` column stays as-is (BLOB, nullable, unused)

---

## Verification Strategy

> **UNIVERSAL RULE: ZERO HUMAN INTERVENTION**
>
> ALL tasks in this plan MUST be verifiable WITHOUT any human action.

### Test Decision

- **Infrastructure exists**: YES (vitest, 9 test files across `packages/graph/src/`)
- **Automated tests**: YES (tests alongside implementation — update existing + add new)
- **Framework**: vitest (already configured)
- **Test command**: `pnpm turbo test:unit`

### Agent-Executed QA Scenarios (MANDATORY — ALL tasks)

Every task includes both vitest assertions and bash-executed verification commands. No human
involvement at any point.

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Start Immediately):
└── Task 1: Type system foundation (enums, interfaces, constraint matrix)

Wave 2 (After Wave 1):
└── Task 2: Schema DDL + FTS5 rebuild + migration

Wave 3 (After Wave 2 — parallel):
├── Task 3: storeNode overhaul (facets, batch, minimal, dedup warning)
├── Task 4: createEdge overhaul (new types, REPLACES behavior, batch, minimal)
└── Task 5: Search enhancements (explainability, sanitizer, tag_mode, multi-type)

Wave 4 (After Tasks 3+4):
└── Task 6: replace_node tool

Wave 5 (After all):
└── Task 7: SKILL.md update
```

### Dependency Matrix

| Task | Depends On | Blocks  | Can Parallelize With |
| ---- | ---------- | ------- | -------------------- |
| 1    | None       | 2       | None (foundational)  |
| 2    | 1          | 3, 4, 5 | None                 |
| 3    | 2          | 6       | 4, 5                 |
| 4    | 2          | 6       | 3, 5                 |
| 5    | 2          | 7       | 3, 4                 |
| 6    | 3, 4       | 7       | None                 |
| 7    | 3, 4, 5, 6 | None    | None (final)         |

### Agent Dispatch Summary

| Wave | Tasks   | Recommended Agents                                                         |
| ---- | ------- | -------------------------------------------------------------------------- |
| 1    | 1       | `task(category="quick", load_skills=["texere"])`                           |
| 2    | 2       | `task(category="unspecified-low", load_skills=["texere"])`                 |
| 3    | 3, 4, 5 | Three parallel `task(category="unspecified-high", load_skills=["texere"])` |
| 4    | 6       | `task(category="unspecified-low", load_skills=["texere"])`                 |
| 5    | 7       | `task(category="writing", load_skills=["texere"])`                         |

---

## TODOs

- [ ] 1. Type System Foundation

  **What to do**:

  Replace the current 17-value `NodeType` enum with 6 core types. Replace 14-value `EdgeType` enum
  with 12 types. Add `NodeRole` enum with strict type-role constraint matrix. Add facet type enums.
  Update all TypeScript interfaces.

  **New `NodeType` enum** (6 values):

  ```typescript
  enum NodeType {
    Knowledge = 'knowledge',
    Issue = 'issue',
    Action = 'action',
    Artifact = 'artifact',
    Context = 'context',
    Meta = 'meta',
  }
  ```

  **New `NodeRole` enum** with type-role constraint matrix:

  ```
  knowledge → decision | requirement | constraint | principle | finding | research
  issue     → problem | error
  action    → task | solution | fix | workflow | command
  artifact  → example | code_pattern | technology | project | file_context
  context   → conversation
  meta      → system
  ```

  **New `EdgeType` enum** (12 values):

  ```typescript
  enum EdgeType {
    Resolves = 'RESOLVES', // was SOLVES, absorbs VALIDATES
    Causes = 'CAUSES', // absorbs MOTIVATED_BY (reverse direction)
    DependsOn = 'DEPENDS_ON', // was REQUIRES
    Extends = 'EXTENDS', // was BUILDS_ON, absorbs IMPLEMENTS
    Constrains = 'CONSTRAINS', // absorbs PREVENTS
    Contradicts = 'CONTRADICTS', // unchanged
    Replaces = 'REPLACES', // was DEPRECATED_BY, keeps auto-invalidation
    AnchoredTo = 'ANCHORED_TO', // unchanged, keeps auto-creation
    AlternativeTo = 'ALTERNATIVE_TO', // kept (validated as needed)
    ExampleOf = 'EXAMPLE_OF', // new
    Supports = 'SUPPORTS', // new
    PartOf = 'PART_OF', // new
  }
  ```

  **New facet types**:

  ```typescript
  enum NodeSource {
    Internal = 'internal',
    External = 'external',
  }
  enum NodeStatus {
    Proposed = 'proposed',
    Active = 'active',
    Deprecated = 'deprecated',
    Invalidated = 'invalidated',
  }
  enum NodeScope {
    Project = 'project',
    Module = 'module',
    File = 'file',
    Session = 'session',
  }
  ```

  **Updated `Node` interface** — add fields:
  - `role: NodeRole` (required)
  - `source: NodeSource` (default: `internal`)
  - `status: NodeStatus` (default: `active`)
  - `scope: NodeScope` (default: `project`)

  **Type-role validation function**: Export a `isValidTypeRole(type, role): boolean` function and a
  `VALID_ROLES_BY_TYPE` constant map that other modules can import.

  **New `SearchResult` type**: `Node` plus `rank`, `match_quality`, `match_fields`, `relationships`.
  Keep separate from `Node` — search returns `SearchResult[]`, not `Node[]`.

  **Updated `SearchOptions` type**:
  - `type` accepts `NodeType | NodeType[]` (single or array)
  - Add `tagMode?: 'all' | 'any'` (default `'all'`)
  - Add `role?: NodeRole` (optional filter)

  **Must NOT do**:
  - Do NOT add facets beyond role, source, status, scope
  - Do NOT change the `Edge` interface (it stays the same)
  - Do NOT touch any implementation files — this task is types only

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Pure type definitions, no logic, single file changes
  - **Skills**: [`texere`]
    - `texere`: Project context for understanding current type system

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 1 (alone)
  - **Blocks**: Task 2
  - **Blocked By**: None

  **References**:

  **Pattern References**:
  - `packages/graph/src/types.ts` — Current enums and interfaces. Replace entirely. The 17-value
    NodeType (lines 1-19) becomes 6 values. The 14-value EdgeType (lines 21-36) becomes 12 values.
    Node interface (lines 38-49) gets 4 new facet fields. SearchOptions (lines 66-72) gets tagMode
    and multi-type support.

  **Test References**:
  - `packages/graph/src/types.test.ts` — Count assertions: "has exactly 17 node types" (line 7) must
    become 6, "has exactly 14 edge types" (line 11) must become 12. Add tests for new enums
    (NodeRole, NodeSource, NodeStatus, NodeScope) and for `isValidTypeRole()` constraint function.

  **Acceptance Criteria**:
  - [ ] `NodeType` enum has exactly 6 values
  - [ ] `EdgeType` enum has exactly 12 values
  - [ ] `NodeRole` enum has exactly 20 values (sum of all valid roles across types)
  - [ ] `isValidTypeRole('knowledge', 'decision')` returns `true`
  - [ ] `isValidTypeRole('knowledge', 'problem')` returns `false`
  - [ ] `isValidTypeRole('issue', 'error')` returns `true`
  - [ ] `isValidTypeRole('artifact', 'file_context')` returns `true`
  - [ ] `Node` interface includes `role`, `source`, `status`, `scope` fields
  - [ ] `SearchResult` type extends `Node` with `rank`, `match_quality`, `match_fields`,
        `relationships`
  - [ ] `SearchOptions.type` accepts `NodeType | NodeType[]`
  - [ ] `SearchOptions.tagMode` exists with type `'all' | 'any'`

  **Agent-Executed QA Scenarios:**

  ```
  Scenario: Type system compiles and tests pass
    Tool: Bash
    Preconditions: None
    Steps:
      1. pnpm turbo test:unit --filter=@texere/graph 2>&1
      2. Assert: types.test.ts passes
      3. Assert: no TypeScript compilation errors
    Expected Result: All type tests pass
    Evidence: Terminal output captured
  ```

  **Commit**: YES
  - Message: `refactor(graph): simplify type system — 6 node types with facets, 12 edge types`
  - Files: `packages/graph/src/types.ts`, `packages/graph/src/types.test.ts`
  - Pre-commit: `pnpm turbo test:unit --filter=@texere/graph`

---

- [ ] 2. Schema DDL, FTS5 Rebuild & Migration

  **What to do**:

  Update the SQLite schema to support new facet columns. FTS5 virtual tables CANNOT be ALTERed —
  must DROP and CREATE fresh. Update all triggers. Write a migration function that handles both
  fresh databases and existing databases with data.

  **Schema changes to `nodes` table**:
  - Add column: `role TEXT NOT NULL`
  - Add column: `source TEXT NOT NULL DEFAULT 'internal'`
  - Add column: `status TEXT NOT NULL DEFAULT 'active'`
  - Add column: `scope TEXT NOT NULL DEFAULT 'project'`

  **FTS5 virtual table** — DROP and recreate:

  ```sql
  DROP TABLE IF EXISTS nodes_fts;
  CREATE VIRTUAL TABLE nodes_fts USING fts5(
    title, content, tags, role,
    content='', tokenize='unicode61'
  );
  ```

  Add `role` to FTS5 so agents can search by role via full-text. Keep `source`, `status`, `scope` as
  WHERE-filterable only (not FTS5-indexed).

  **Updated triggers** — must include `role` in FTS5 inserts:

  ```sql
  -- nodes_fts_ai: INSERT INTO nodes_fts(rowid, title, content, tags, role)
  -- nodes_fts_ad: same with 'delete' command
  ```

  **New indexes**:

  ```sql
  CREATE INDEX idx_nodes_role ON nodes(role) WHERE invalidated_at IS NULL;
  CREATE INDEX idx_nodes_status ON nodes(status) WHERE invalidated_at IS NULL;
  ```

  **Migration strategy**: The schema initialization must handle two cases:
  1. **Fresh DB**: Create tables with new schema directly
  2. **Existing DB with data**: Detect existing schema, migrate data, rebuild FTS5

  For existing DBs (20 nodes, 7 edges):
  - Map old node types to new type+role (e.g., `decision` → `knowledge`/`decision`, `problem` →
    `issue`/`problem`, `general` → `knowledge`/`finding`, `file_context` →
    `artifact`/`file_context`)
  - Map old edge types to new types (e.g., `SOLVES` → `RESOLVES`, `REQUIRES` → `DEPENDS_ON`,
    `DEPRECATED_BY` → `REPLACES`, `BUILDS_ON` → `EXTENDS`, `RELATED_TO` → delete, `MOTIVATED_BY` →
    delete or convert to `CAUSES` with flipped direction)
  - Repopulate FTS5 from migrated data

  **Must NOT do**:
  - Do NOT change any function implementations yet — just the DDL and migration
  - Do NOT add columns that aren't in the decided facet set (role, source, status, scope)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-low`
    - Reason: Schema DDL changes and migration logic, moderate complexity
  - **Skills**: [`texere`]
    - `texere`: Understanding of current schema and data

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 2 (alone)
  - **Blocks**: Tasks 3, 4, 5
  - **Blocked By**: Task 1

  **References**:

  **Pattern References**:
  - `packages/graph/src/schema.ts` — Current DDL (lines 1-69). The entire `SCHEMA_DDL` string must
    be updated. Note the FTS5 table (lines 32-38), triggers (lines 48-68), and indexes (lines
    40-46). FTS5 uses `content=''` (contentless) meaning data is NOT stored in the FTS table — only
    indexed. Rebuild requires reinserting from main `nodes` table.
  - `packages/graph/src/db.ts` — Database initialization, where SCHEMA_DDL is executed. Migration
    logic should be called here.

  **Type References**:
  - `packages/graph/src/types.ts` — Updated types from Task 1. The Node interface now has `role`,
    `source`, `status`, `scope` fields that need corresponding DDL columns.

  **Test References**:
  - `packages/graph/src/db.test.ts` — Tests database initialization. Add migration test: create DB
    with old schema, insert old-format data, run migration, verify new schema.
  - `packages/graph/src/index.int.test.ts` — Integration test uses old NodeType/EdgeType values
    (lines 16-17: `NodeType.Task`, `EdgeType.RelatedTo`). Must be updated to new types.

  **Acceptance Criteria**:
  - [ ] Fresh DB: `nodes` table has columns `role`, `source`, `status`, `scope`
  - [ ] Fresh DB: FTS5 table indexes `title`, `content`, `tags`, `role`
  - [ ] Fresh DB: Triggers insert `role` into FTS5
  - [ ] Existing DB: Old `decision` type nodes become `knowledge`/`decision`
  - [ ] Existing DB: Old `problem` type nodes become `issue`/`problem`
  - [ ] Existing DB: Old `general` type nodes become `knowledge`/`finding`
  - [ ] Existing DB: Old `file_context` type nodes become `artifact`/`file_context`
  - [ ] Existing DB: Old `SOLVES` edges become `RESOLVES`
  - [ ] Existing DB: Old `RELATED_TO` edges deleted (or best-effort mapped)
  - [ ] Existing DB: FTS5 repopulated after migration
  - [ ] `pnpm turbo test:unit --filter=@texere/graph` passes

  **Agent-Executed QA Scenarios:**

  ```
  Scenario: Fresh database has new schema
    Tool: Bash
    Preconditions: No existing database
    Steps:
      1. Create in-memory DB via TextereDB(':memory:')
      2. Query: PRAGMA table_info(nodes)
      3. Assert: columns include role, source, status, scope
      4. Query: SELECT * FROM sqlite_master WHERE name='nodes_fts'
      5. Assert: FTS5 table exists with role column
    Expected Result: Schema includes all new columns and rebuilt FTS5
    Evidence: Test output captured

  Scenario: Migration handles old data
    Tool: Bash
    Preconditions: Database with old-schema data
    Steps:
      1. Create DB with old schema, insert node with type='decision'
      2. Run migration
      3. Query: SELECT type, role FROM nodes WHERE id = ?
      4. Assert: type='knowledge', role='decision'
    Expected Result: Old data correctly migrated to new schema
    Evidence: Test output captured
  ```

  **Commit**: YES
  - Message: `refactor(graph): update schema DDL with facet columns, rebuild FTS5, add migration`
  - Files: `packages/graph/src/schema.ts`, `packages/graph/src/db.ts`,
    `packages/graph/src/db.test.ts`, `packages/graph/src/index.int.test.ts`
  - Pre-commit: `pnpm turbo test:unit --filter=@texere/graph`

---

- [ ] 3. storeNode Overhaul

  **What to do**:

  Update `storeNode` to handle new type system with facets, support batch input (single or array,
  always atomic), add minimal response mode, add duplicate warning, and update `anchor_to` to use
  new types. Update the MCP tool Zod schema to match.

  **Type-role validation**: Call `isValidTypeRole(type, role)` from Task 1. Throw before opening
  transaction if invalid. This is enforced at the graph layer, not just Zod.

  **Facet handling**:
  - `role` — required, validated against type
  - `source` — optional, default `'internal'`
  - `status` — optional, default `'active'`
  - `scope` — optional, default `'project'`

  **Batch support**:
  - `storeNode` accepts `StoreNodeInput | StoreNodeInput[]`
  - Normalize single input to `[input]` internally
  - Pre-validate ALL items before opening transaction (fail fast)
  - Single transaction for entire batch (all-or-nothing)
  - Max batch size: 50 (validate, throw if exceeded)
  - Empty array: throw validation error ("at least one node required")
  - Return: single node for single input, array for array input (match input shape)

  **Minimal response mode**:
  - Add `minimal?: boolean` to input options (not per-node, per-call)
  - Default: `false` (current verbose behavior)
  - When `true`: return only `{ id }` per node
  - `minimal: true` suppresses duplicate warnings

  **Duplicate warning**:
  - Before insert, search FTS5 on title field for similar nodes
  - If matches found: include `warning: { similar_nodes: [{ id, title, match_quality }] }` in
    response
  - Does NOT block creation — informational only
  - Skipped when `minimal: true`
  - Skipped for batch creates (you explicitly constructed the batch)
  - Skipped for `anchor_to` auto-created nodes

  **anchor_to update**:
  - Auto-created file context nodes: `type: NodeType.Artifact`, `role: 'file_context'`
  - Keep `INSERT OR IGNORE` behavior (idempotent)
  - Keep deterministic ID format `file_context:${path}`
  - Must also insert new facet columns (source: internal, status: active, scope: file)

  **MCP tool update** (`apps/mcp/src/tools/store-node.ts`):
  - Add `role` to Zod schema (required)
  - Add `source`, `status`, `scope` (optional with defaults)
  - Add `minimal` (optional boolean)
  - Accept single object or array: `z.union([singleSchema, z.array(singleSchema)])`

  **Must NOT do**:
  - Do NOT add general update semantics
  - Do NOT make duplicate detection more sophisticated than FTS5 title search
  - Do NOT make batch size configurable — hardcode 50

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Multiple interacting features (facets, batch, minimal, dedup, anchor_to) in one
      function, significant logic changes
  - **Skills**: [`texere`]
    - `texere`: Understanding of current storeNode implementation and MCP tool pattern

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 3 (with Tasks 4, 5)
  - **Blocks**: Task 6
  - **Blocked By**: Task 2

  **References**:

  **Pattern References**:
  - `packages/graph/src/nodes.ts` — Current `storeNode` implementation (lines 144-194). Note:
    `StoreNodeInput` interface (lines 6-15), prepared statements cache pattern (lines 34-142),
    transaction pattern (lines 162-191), anchor_to auto-creation (lines 165-189 — creates
    `NodeType.FileContext` nodes and `EdgeType.AnchoredTo` edges).
  - `packages/graph/src/edges.ts:66-87` — `createEdge` transaction pattern. Follow same pattern for
    batch: `db.transaction(() => { ... }).immediate()`.

  **API/Type References**:
  - `packages/graph/src/types.ts` — Updated types from Task 1. `StoreNodeInput` needs `role`
    (required) and optional facets. Return type: `Node` or `Node[]` depending on input shape, or
    `{ id }` / `{ id }[]` when minimal.

  **MCP Tool References**:
  - `apps/mcp/src/tools/store-node.ts` — Current MCP tool (47 lines). Zod schema (lines 8-16) needs
    `role`, facets, `minimal`. Execute function (lines 22-45) needs batch handling.
  - `apps/mcp/src/tools/helpers.ts` — `ok()` helper for response formatting. May need a
    `okMinimal()` variant or conditional logic.

  **Test References**:
  - `packages/graph/src/nodes.test.ts` — Existing node tests. Add tests for: valid type-role
    succeeds, invalid type-role throws, batch creates atomically, batch rollback on failure, minimal
    response shape, duplicate warning, anchor_to with new types.

  **Acceptance Criteria**:
  - [ ] `storeNode({ type: 'knowledge', role: 'decision', ... })` succeeds
  - [ ] `storeNode({ type: 'knowledge', role: 'problem', ... })` throws validation error
  - [ ] `storeNode([nodeA, nodeB, nodeC])` creates all 3 atomically
  - [ ] `storeNode([nodeA, INVALID, nodeC])` creates 0 nodes (all-or-nothing)
  - [ ] `storeNode([])` throws "at least one node required"
  - [ ] Batch of 51 items throws "max batch size exceeded"
  - [ ] `storeNode({ ..., minimal: true })` returns `{ id: "<nanoid>" }` only
  - [ ] Creating node with similar title returns `warning.similar_nodes` array
  - [ ] Duplicate warning absent when `minimal: true`
  - [ ] `anchor_to` creates nodes with type `artifact`, role `file_context`
  - [ ] `source` defaults to `internal`, `status` to `active`, `scope` to `project`
  - [ ] MCP tool accepts `role` in input, validates against type
  - [ ] `pnpm turbo test:unit --filter=@texere/graph` passes

  **Agent-Executed QA Scenarios:**

  ```
  Scenario: Batch create is atomic — success case
    Tool: Bash
    Preconditions: Empty database
    Steps:
      1. Call storeNode with array of 3 valid nodes
      2. Assert: returns array of 3 nodes with IDs
      3. Query: SELECT COUNT(*) FROM nodes
      4. Assert: count is 3
    Expected Result: All 3 nodes created in one transaction
    Evidence: Test output captured

  Scenario: Batch create is atomic — failure case
    Tool: Bash
    Preconditions: Empty database
    Steps:
      1. Call storeNode with [valid, invalid_role, valid]
      2. Assert: throws error mentioning invalid role
      3. Query: SELECT COUNT(*) FROM nodes
      4. Assert: count is 0
    Expected Result: Zero nodes created on batch failure
    Evidence: Test output captured

  Scenario: Duplicate warning fires on similar title
    Tool: Bash
    Preconditions: Node with title "Redis connection pooling" exists
    Steps:
      1. Call storeNode({ title: "Redis connection pooling fix", ... })
      2. Assert: response includes warning.similar_nodes array
      3. Assert: warning contains existing node's ID
      4. Assert: new node IS created (non-blocking)
    Expected Result: Warning present but creation not blocked
    Evidence: Test output captured
  ```

  **Commit**: YES
  - Message: `feat(graph): storeNode — facets, batch support, minimal mode, duplicate warning`
  - Files: `packages/graph/src/nodes.ts`, `packages/graph/src/nodes.test.ts`,
    `apps/mcp/src/tools/store-node.ts`
  - Pre-commit: `pnpm turbo test:unit`

---

- [ ] 4. createEdge Overhaul

  **What to do**:

  Update `createEdge` to handle the new 12 edge types, port `DEPRECATED_BY` auto-invalidation
  behavior to `REPLACES`, add batch support (single or array, always atomic), and add minimal
  response mode. Update the MCP tool Zod schema.

  **Edge type changes**:
  - `EdgeType.DeprecatedBy` → `EdgeType.Replaces`
  - Auto-invalidation behavior (edges.ts:81-83) must trigger on `EdgeType.Replaces` instead
  - All other edge types: no special behavior, just insert

  **Batch support**:
  - Same pattern as storeNode: accept single or array, normalize internally
  - Pre-validate all items before transaction
  - Max batch size: 50
  - Empty array: throw validation error
  - Return: match input shape (single → single, array → array)
  - `REPLACES` auto-invalidation works within batch (if batch contains a REPLACES edge, the source
    node gets invalidated within the same transaction)

  **Minimal response mode**:
  - `minimal?: boolean` on input (per-call, not per-edge)
  - Default: `false`
  - When `true`: return `{ id }` per edge

  **MCP tool update** (`apps/mcp/src/tools/create-edge.ts`):
  - Update Zod `type` to use new `EdgeType` enum (automatic via `z.nativeEnum`)
  - Accept single or array: `z.union([singleSchema, z.array(singleSchema)])`
  - Add `minimal` option

  **Must NOT do**:
  - Do NOT add edge context/description field
  - Do NOT add any other auto-behaviors beyond REPLACES invalidation

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Multiple features (batch, minimal, type migration) with transaction logic
  - **Skills**: [`texere`]
    - `texere`: Understanding of edge creation and auto-invalidation behavior

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 3 (with Tasks 3, 5)
  - **Blocks**: Task 6
  - **Blocked By**: Task 2

  **References**:

  **Pattern References**:
  - `packages/graph/src/edges.ts` — Current `createEdge` (lines 66-87). The auto-invalidation at
    lines 81-83 (`if (edge.type === EdgeType.DeprecatedBy)`) must change to `EdgeType.Replaces`. The
    `invalidateNode` prepared statement (line 38-39) stays the same. Batch pattern: wrap the loop in
    the existing `db.transaction(() => { ... }).immediate()`.
  - `packages/graph/src/nodes.ts:162-191` — storeNode's transaction pattern for reference on batch
    transaction structure.

  **API/Type References**:
  - `packages/graph/src/types.ts` — Updated `EdgeType` enum from Task 1. `CreateEdgeInput` interface
    (edges.ts:6-12) stays the same structurally, just the `type` field accepts new enum values.

  **MCP Tool References**:
  - `apps/mcp/src/tools/create-edge.ts` — Current MCP tool (33 lines). Zod schema (lines 8-14)
    auto-updates via `z.nativeEnum(EdgeType)`. Add array support and `minimal` option.

  **Test References**:
  - `packages/graph/src/edges.test.ts` — Existing edge tests. Add tests for: REPLACES triggers
    auto-invalidation, batch creates atomically, batch rollback, minimal response.
  - `packages/graph/src/index.int.test.ts` — Uses `EdgeType.Solves` (line 64) and
    `EdgeType.BuildsOn` (line 72). Must update to `EdgeType.Resolves` and `EdgeType.Extends`.

  **Acceptance Criteria**:
  - [ ] `createEdge({ type: 'REPLACES', ... })` auto-invalidates source node
  - [ ] `createEdge({ type: 'RESOLVES', ... })` does NOT auto-invalidate
  - [ ] `createEdge([edgeA, edgeB])` creates both atomically
  - [ ] `createEdge([valid, INVALID_REF, valid])` creates 0 edges
  - [ ] `createEdge([])` throws "at least one edge required"
  - [ ] Batch of 51 throws "max batch size exceeded"
  - [ ] `createEdge({ ..., minimal: true })` returns `{ id }` only
  - [ ] `EdgeType.Replaces` exists, `EdgeType.DeprecatedBy` does not
  - [ ] `EdgeType.Resolves` exists, `EdgeType.Solves` does not
  - [ ] `pnpm turbo test:unit` passes

  **Agent-Executed QA Scenarios:**

  ```
  Scenario: REPLACES edge auto-invalidates source node
    Tool: Bash
    Preconditions: Two nodes exist (old and new)
    Steps:
      1. createEdge({ source_id: old.id, target_id: new.id, type: 'REPLACES' })
      2. Query: SELECT invalidated_at FROM nodes WHERE id = old.id
      3. Assert: invalidated_at is NOT NULL
    Expected Result: Old node auto-invalidated by REPLACES edge
    Evidence: Test output captured

  Scenario: Batch edge create with REPLACES in batch
    Tool: Bash
    Preconditions: Three nodes exist
    Steps:
      1. createEdge([
           { source: A, target: B, type: 'RESOLVES' },
           { source: C, target: A, type: 'REPLACES' }
         ])
      2. Assert: both edges created
      3. Assert: node C has invalidated_at set (REPLACES triggered)
      4. Assert: node A does NOT have invalidated_at set
    Expected Result: Auto-invalidation works correctly within batch
    Evidence: Test output captured
  ```

  **Commit**: YES
  - Message: `feat(graph): createEdge — new types, REPLACES behavior, batch support, minimal mode`
  - Files: `packages/graph/src/edges.ts`, `packages/graph/src/edges.test.ts`,
    `apps/mcp/src/tools/create-edge.ts`
  - Pre-commit: `pnpm turbo test:unit`

---

- [ ] 5. Search Enhancements

  **What to do**:

  Major search overhaul: expose BM25 rank, add match_quality normalization, detect match_fields, add
  relationship summary counts, relax FTS5 sanitizer with fallback, add tag OR logic, add multi-type
  filtering, fix tag-only search bug. Update MCP tools for search and about.

  **BM25 rank exposure** (Decision 3):
  - Stop stripping rank at `search.ts:77`. Currently: `rows.map(({ rank: _rank, ...node }) => node)`
  - Instead, include `rank` in results
  - BM25 scores are negative (more negative = worse). Normalize to `match_quality` (0-1):
    `match_quality = 1 / (1 + Math.abs(rank))`

  **match_fields detection** (Decision 3):
  - After FTS5 match, determine which columns matched by running `snippet()` or by checking which
    FTS5 columns contain the query terms
  - Simpler approach: run separate MATCH queries per column (title, content, tags) and report which
    returned the result. Or use FTS5 `highlight()` to detect which columns had matches.
  - Return as `match_fields: ['title', 'content']`

  **Relationship summary** (Decision 4):
  - For each search result, aggregate edge counts by type and direction
  - Use a single JOIN query, not N+1:
    ```sql
    SELECT
      n.id,
      e.type AS edge_type,
      CASE WHEN e.source_id = n.id THEN 'outgoing' ELSE 'incoming' END AS direction,
      COUNT(*) AS cnt
    FROM nodes n
    JOIN edges e ON e.source_id = n.id OR e.target_id = n.id
    WHERE n.id IN (... search result ids ...)
    GROUP BY n.id, e.type, direction
    ```
  - Return as `relationships: { outgoing: { RESOLVES: 1 }, incoming: { CAUSES: 2 } }`

  **FTS5 sanitizer relaxation** (Decision 9):
  - Modify `sanitizeFtsQuery` → rename to `sanitizeFtsQueryStrict` (current behavior)
  - New approach: try raw query first, catch FTS5 syntax error, fall back to strict sanitization
  - In `search()`: wrap the query execution in try/catch. On failure, re-sanitize and retry.
  - Phrases (`"exact match"`), booleans (`OR`, `AND`, `NOT`), grouping (`()`), `NEAR` all pass
    through on first attempt

  **Tag OR logic** (Decision 10):
  - Current `search.ts:43-52`: `HAVING COUNT(DISTINCT tag) = @tagCount` enforces AND
  - When `tagMode: 'any'`: change to `HAVING COUNT(DISTINCT tag) >= 1`
  - Default: `'all'` (current behavior, no regression)

  **Multi-type filtering** (Decision 11):
  - Current `search.ts:20-22`: `n.type = @type` (single value)
  - When type is array: `n.type IN (@type0, @type1, ...)` with parameterized values
  - Single string still works (no regression)

  **Tag-only search bug fix** (Metis E5):
  - Current `search.ts:10-12`: returns `[]` when query is empty, even if tags are provided
  - Fix: when query is empty but tags/type/minImportance filters exist, skip FTS5 MATCH and query
    directly against `nodes` table with filters only
  - This requires a second query path (non-FTS5) for filter-only searches

  **Return type change**:
  - `search()` returns `SearchResult[]` (from Task 1) instead of `Node[]`
  - `about()` also needs update since it calls `search()` internally

  **MCP tool updates**:
  - `apps/mcp/src/tools/search.ts` — add `tag_mode`, update `type` to accept array, add `role`
    filter
  - `apps/mcp/src/tools/about.ts` — same `type`/`tag_mode`/`role` changes, response shape change

  **Must NOT do**:
  - Do NOT add embedding/vector search
  - Do NOT add fuzzy/typo tolerance
  - Do NOT change `about` beyond what search changes require

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Most complex task — multiple interacting search features, SQL query construction, FTS5
      behavior, new return types
  - **Skills**: [`texere`]
    - `texere`: Understanding of FTS5 behavior, BM25 scoring, query construction

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 3 (with Tasks 3, 4)
  - **Blocks**: Task 7
  - **Blocked By**: Task 2

  **References**:

  **Pattern References**:
  - `packages/graph/src/search.ts` — Entire file (79 lines). BM25 computed at line 67, stripped at
    line 77. Tag filtering JOIN at lines 44-52 (AND logic via `HAVING COUNT = @tagCount`). Type
    filtering at lines 20-22 (single type). Empty query early return at lines 10-12 (the bug). SQL
    construction pattern at lines 55-74.
  - `packages/graph/src/sanitize.ts` — Current sanitizer (22 lines). Line 8 strips quotes, lines
    13-15 escape boolean operators as literals. This becomes the fallback path.
  - `packages/graph/src/about.ts` — Calls `search()` internally. Return type change propagates.

  **API/Type References**:
  - `packages/graph/src/types.ts` — `SearchResult` type from Task 1 (Node + rank + match_quality
    - match_fields + relationships). `SearchOptions` updated with `tagMode`, array `type`.

  **MCP Tool References**:
  - `apps/mcp/src/tools/search.ts` — Zod schema (lines 8-14). Add `tag_mode`, update `type` to
    accept single or array, add `role` filter.
  - `apps/mcp/src/tools/about.ts` — Same parameter changes. Response includes new search fields.

  **Test References**:
  - `packages/graph/src/search.test.ts` — Existing search tests. Add tests for: rank included,
    match_quality normalized, match_fields populated, relationship counts, tag_mode any/all,
    multi-type filter, tag-only search (no query), FTS5 phrase query, FTS5 boolean query, FTS5
    fallback on invalid syntax.
  - `packages/graph/src/sanitize.test.ts` — Existing sanitizer tests. Add tests for: raw query
    passthrough, fallback behavior.

  **Acceptance Criteria**:
  - [ ] Search results include `rank` (number, negative BM25 score)
  - [ ] Search results include `match_quality` (number, 0-1 normalized)
  - [ ] Search results include `match_fields` (string array, non-empty)
  - [ ] Search results include `relationships` (object with outgoing/incoming edge counts)
  - [ ] `search({ query: '"exact phrase"' })` uses FTS5 phrase matching
  - [ ] `search({ query: 'foo OR bar' })` uses FTS5 boolean OR
  - [ ] `search({ query: 'foo AND AND bar' })` falls back to sanitized query (no error)
  - [ ] `search({ tags: ["redis"], tag_mode: "any" })` returns nodes with ANY matching tag
  - [ ] `search({ tags: ["redis", "sql"], tag_mode: "all" })` returns nodes with BOTH tags
  - [ ] `search({ type: ["knowledge", "action"] })` returns nodes of either type
  - [ ] `search({ query: "", tags: ["redis"] })` returns matching nodes (bug fix, was returning [])
  - [ ] `search({ query: "test", type: "knowledge" })` still works (single string, no regression)
  - [ ] `about()` results include new search fields
  - [ ] `pnpm turbo test:unit` passes

  **Agent-Executed QA Scenarios:**

  ```
  Scenario: FTS5 phrase search works
    Tool: Bash
    Preconditions: Nodes exist with "connection timeout" in content
    Steps:
      1. search({ query: '"connection timeout"' })
      2. Assert: returns nodes containing exact phrase
      3. search({ query: '"timeout connection"' })
      4. Assert: returns empty or different results (phrase order matters)
    Expected Result: Phrase search respects word order
    Evidence: Test output captured

  Scenario: FTS5 fallback on invalid syntax
    Tool: Bash
    Preconditions: Nodes exist
    Steps:
      1. search({ query: 'foo AND AND bar' })
      2. Assert: does NOT throw error
      3. Assert: returns results (fell back to sanitized query)
    Expected Result: Invalid FTS5 syntax gracefully falls back
    Evidence: Test output captured

  Scenario: Tag-only search returns results
    Tool: Bash
    Preconditions: Node with tag "redis" exists
    Steps:
      1. search({ query: "", tags: ["redis"] })
      2. Assert: returns node(s) with "redis" tag
      3. Assert: result count > 0
    Expected Result: Filter-only search works without FTS5 query
    Evidence: Test output captured
  ```

  **Commit**: YES
  - Message: `feat(graph): search — explainability, FTS5 power, tag_mode, multi-type, tag-only fix`
  - Files: `packages/graph/src/search.ts`, `packages/graph/src/sanitize.ts`,
    `packages/graph/src/search.test.ts`, `packages/graph/src/sanitize.test.ts`,
    `apps/mcp/src/tools/search.ts`, `apps/mcp/src/tools/about.ts`
  - Pre-commit: `pnpm turbo test:unit`

---

- [ ] 6. replace_node Tool

  **What to do**:

  Create a new `texere_replace_node` tool that atomically performs three operations: store a new
  node, create a `REPLACES` edge from old to new, and invalidate the old node. This collapses a
  3-call workflow into 1 call.

  **Graph layer** — new function in `packages/graph/src/replace-node.ts`:

  ```typescript
  export const replaceNode = (db, input: { old_id: string } & StoreNodeInput): Node => {
    return db
      .transaction(() => {
        // 1. Verify old node exists and is not already invalidated
        const old = getNode(db, input.old_id);
        if (!old) throw new Error(`Node not found: ${input.old_id}`);

        // 2. Store new node
        const newNode = storeNode(db, { ...input });

        // 3. Create REPLACES edge (new → old)
        createEdge(db, {
          source_id: input.old_id,
          target_id: newNode.id,
          type: EdgeType.Replaces,
        });
        // Note: REPLACES auto-invalidation (from Task 4) handles step 4 automatically

        return newNode;
      })
      .immediate();
  };
  ```

  **Edge direction**: `old_id → new_id` with type `REPLACES` — meaning "old is replaced by new."
  This matches the `DEPRECATED_BY` convention where the source is the deprecated node.

  **MCP tool** — new file `apps/mcp/src/tools/replace-node.ts`:
  - Input: `old_id` (required string) + all `storeNode` fields (type, role, title, content, etc.)
  - Add `minimal` option
  - Register in `apps/mcp/src/tools/index.ts` TOOL_DEFINITIONS array

  **Expose on TextereDB class**: Add `replaceNode` method alongside existing `storeNode`,
  `createEdge`.

  **Must NOT do**:
  - Do NOT add partial update semantics — this is replace, not update
  - Do NOT allow replacing an already-invalidated node (throw error)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-low`
    - Reason: Straightforward composition of existing functions, small scope
  - **Skills**: [`texere`]
    - `texere`: Understanding of storeNode, createEdge, and MCP tool registration pattern

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 4 (alone)
  - **Blocks**: Task 7
  - **Blocked By**: Tasks 3, 4

  **References**:

  **Pattern References**:
  - `packages/graph/src/nodes.ts:144-194` — `storeNode` implementation. `replaceNode` calls this
    internally. Follow the same module pattern: private `getStatements`, exported function.
  - `packages/graph/src/edges.ts:66-87` — `createEdge` with auto-invalidation. `replaceNode` relies
    on `REPLACES` auto-invalidation from Task 4.
  - `packages/graph/src/nodes.ts:216-233` — `invalidateNode`. The auto-invalidation from the
    REPLACES edge handles this, so `replaceNode` should NOT call `invalidateNode` directly.

  **MCP Tool References**:
  - `apps/mcp/src/tools/store-node.ts` — Follow this pattern exactly for the new tool. Zod schema,
    `ToolDefinition` type, `ok()` response helper.
  - `apps/mcp/src/tools/index.ts` — Register new tool in `TOOL_DEFINITIONS` array (line 14-24).
    Import and add `replaceNodeTool`.

  **Test References**:
  - `packages/graph/src/index.int.test.ts` — Integration test pattern. Add a `replaceNode` scenario
    to the lifecycle test.

  **Acceptance Criteria**:
  - [ ] `replaceNode({ old_id: X, type: 'knowledge', role: 'decision', ... })` creates new node
  - [ ] After replace: old node has `invalidated_at` set
  - [ ] After replace: `REPLACES` edge exists from old → new
  - [ ] After replace: new node is returned (or `{ id }` if minimal)
  - [ ] `replaceNode({ old_id: 'nonexistent', ... })` throws "Node not found"
  - [ ] `replaceNode({ old_id: already_invalidated, ... })` throws error
  - [ ] Operation is atomic: if new node creation fails, old node is NOT invalidated
  - [ ] MCP tool `texere_replace_node` is registered and callable
  - [ ] `pnpm turbo test:unit` passes

  **Agent-Executed QA Scenarios:**

  ```
  Scenario: Atomic replace succeeds
    Tool: Bash
    Preconditions: Node A exists with type knowledge/decision
    Steps:
      1. replaceNode({ old_id: A.id, type: 'knowledge', role: 'decision', title: 'Updated', ... })
      2. Assert: returns new node B with different ID
      3. Assert: node A has invalidated_at set
      4. Assert: edge exists: A → B, type REPLACES
      5. Assert: node B has invalidated_at = null
    Expected Result: Clean atomic replacement
    Evidence: Test output captured

  Scenario: Atomic replace fails safely
    Tool: Bash
    Preconditions: Node A exists
    Steps:
      1. replaceNode({ old_id: A.id, type: 'knowledge', role: 'INVALID_ROLE', ... })
      2. Assert: throws validation error
      3. Assert: node A still has invalidated_at = null (unchanged)
      4. Assert: no new node created
      5. Assert: no new edge created
    Expected Result: Failed replace leaves database unchanged
    Evidence: Test output captured
  ```

  **Commit**: YES
  - Message: `feat(graph): add texere_replace_node atomic helper`
  - Files: `packages/graph/src/replace-node.ts`, `packages/graph/src/index.ts`,
    `apps/mcp/src/tools/replace-node.ts`, `apps/mcp/src/tools/index.ts`
  - Pre-commit: `pnpm turbo test:unit`

---

- [ ] 7. SKILL.md Update

  **What to do**:

  Update `skills/texere/SKILL.md` to document all v1.1 changes. This is the agent-facing
  documentation — it must be accurate and clear for LLM consumption.

  **Sections to update**:
  - **Node type reference table**: Replace 17 types with 6 types + role matrix
  - **Edge type reference table**: Replace 14 types with 12 types, document which old types mapped
    to which new types
  - **New parameters**: Document `role` (required), `source`, `status`, `scope` facets
  - **Batch usage**: Show examples of array input for `store_node` and `create_edge`
  - **Minimal mode**: Document `minimal: true` option
  - **Search improvements**: Document `tag_mode`, multi-type `type` parameter, phrase search,
    boolean operators, match_quality/match_fields/relationships in results
  - **New tool**: Document `texere_replace_node` with usage examples
  - **Updated examples**: All code examples must use new type+role format
  - **Search-before-create workflow**: Already present, verify still accurate

  **Must NOT do**:
  - Do NOT restructure the pedagogical flow of SKILL.md — update in place
  - Do NOT add speculative features not implemented in v1.1
  - Do NOT remove the mandatory search-before-create workflow

  **Recommended Agent Profile**:
  - **Category**: `writing`
    - Reason: Documentation update, needs clear technical writing
  - **Skills**: [`texere`]
    - `texere`: Full understanding of what the skill document needs to convey

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 5 (alone, final)
  - **Blocks**: None
  - **Blocked By**: Tasks 3, 4, 5, 6

  **References**:

  **Pattern References**:
  - `skills/texere/SKILL.md` — Current skill document. The mandatory search-before-create workflow
    section (added in Decision 8) should stay. Type tables, edge tables, examples, and tool
    descriptions all need updating.

  **Source of Truth**:
  - `packages/graph/src/types.ts` — Final enums and interfaces (after Tasks 1-6 complete)
  - `apps/mcp/src/tools/index.ts` — Final tool list (includes `replace_node`)
  - `.sisyphus/drafts/v1.1-planning.md` — All 12 decisions with rationale

  **Acceptance Criteria**:
  - [ ] Node type table shows 6 types with role constraints
  - [ ] Edge type table shows 12 types with descriptions
  - [ ] Examples use `type: 'knowledge', role: 'decision'` format (not old `type: 'decision'`)
  - [ ] `texere_replace_node` documented with usage example
  - [ ] Batch create documented with array input example
  - [ ] `minimal: true` documented
  - [ ] Search parameters `tag_mode`, multi-type `type`, `role` filter documented
  - [ ] FTS5 phrase and boolean query examples shown
  - [ ] No references to removed types (`RELATED_TO`, `MOTIVATED_BY`, old node types)

  **Agent-Executed QA Scenarios:**

  ```
  Scenario: SKILL.md contains no references to removed types
    Tool: Bash
    Preconditions: SKILL.md has been updated
    Steps:
      1. grep -c 'RELATED_TO' skills/texere/SKILL.md
      2. Assert: count is 0
      3. grep -c 'MOTIVATED_BY' skills/texere/SKILL.md
      4. Assert: count is 0
      5. grep -c "type: 'decision'" skills/texere/SKILL.md (old format)
      6. Assert: count is 0 (should be type: 'knowledge', role: 'decision')
    Expected Result: No stale references to v1.0 types
    Evidence: grep output captured

  Scenario: SKILL.md documents all new tools
    Tool: Bash
    Preconditions: SKILL.md has been updated
    Steps:
      1. grep -c 'texere_replace_node' skills/texere/SKILL.md
      2. Assert: count >= 1
      3. grep -c 'tag_mode' skills/texere/SKILL.md
      4. Assert: count >= 1
      5. grep -c 'minimal' skills/texere/SKILL.md
      6. Assert: count >= 1
    Expected Result: All new features documented
    Evidence: grep output captured
  ```

  **Commit**: YES
  - Message: `docs(skill): update SKILL.md for v1.1 — new types, edges, tools, search params`
  - Files: `skills/texere/SKILL.md`
  - Pre-commit: `pnpm turbo test:unit`

---

## Commit Strategy

| After Task | Message                                                                                | Key Files                                                                           | Verification                                  |
| ---------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------- |
| 1          | `refactor(graph): simplify type system — 6 node types with facets, 12 edge types`      | types.ts, types.test.ts                                                             | `pnpm turbo test:unit --filter=@texere/graph` |
| 2          | `refactor(graph): update schema DDL with facet columns, rebuild FTS5, add migration`   | schema.ts, db.ts, db.test.ts, index.int.test.ts                                     | `pnpm turbo test:unit --filter=@texere/graph` |
| 3          | `feat(graph): storeNode — facets, batch support, minimal mode, duplicate warning`      | nodes.ts, nodes.test.ts, store-node.ts                                              | `pnpm turbo test:unit`                        |
| 4          | `feat(graph): createEdge — new types, REPLACES behavior, batch support, minimal mode`  | edges.ts, edges.test.ts, create-edge.ts                                             | `pnpm turbo test:unit`                        |
| 5          | `feat(graph): search — explainability, FTS5 power, tag_mode, multi-type, tag-only fix` | search.ts, sanitize.ts, search.test.ts, sanitize.test.ts, search.ts (MCP), about.ts | `pnpm turbo test:unit`                        |
| 6          | `feat(graph): add texere_replace_node atomic helper`                                   | replace-node.ts, index.ts (both packages)                                           | `pnpm turbo test:unit`                        |
| 7          | `docs(skill): update SKILL.md for v1.1 — new types, edges, tools, search params`       | SKILL.md                                                                            | `pnpm turbo test:unit`                        |

---

## Success Criteria

### Verification Commands

```bash
# Full test suite
pnpm turbo test:unit  # Expected: all tests pass

# Type system
# NodeType enum has 6 values, EdgeType has 12, NodeRole has 20

# Search power
# FTS5 phrase search: search({ query: '"exact phrase"' }) works
# Boolean: search({ query: 'foo OR bar' }) works
# Tag-only: search({ query: "", tags: ["redis"] }) returns results

# Batch atomicity
# storeNode([valid, invalid, valid]) creates 0 nodes

# Replace helper
# replaceNode({ old_id: X, ... }) atomically creates + links + invalidates
```

### Final Checklist

- [ ] All 6 node types functional with role validation
- [ ] All 12 edge types functional
- [ ] Invalid type-role combos rejected at graph layer
- [ ] Batch creates atomic for both nodes and edges
- [ ] Search returns rank, match_quality, match_fields, relationships
- [ ] FTS5 phrases, booleans, grouping work with fallback
- [ ] Tag OR logic works via tag_mode parameter
- [ ] Multi-type filtering works
- [ ] Tag-only search works (bug fixed)
- [ ] replace_node atomic helper works
- [ ] Minimal response mode works for all write tools
- [ ] Duplicate warning fires on similar titles
- [ ] SKILL.md fully updated
- [ ] All existing tests updated and passing
- [ ] No references to removed types anywhere in codebase
